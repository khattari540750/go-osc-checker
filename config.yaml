# OSC Checker Configuration
app:
  name: "OSC Checker"
  version: "1.0.0"

sender:
  default_host: "127.0.0.1"
  default_port: 7000
  default_address: "/test"
  window:
    width: 400
    height: 350
    title: "OSC Sender"

receiver:
  default_port: 7000
  window:
    width: 400
    height: 350
    title: "OSC Receiver"
  max_log_entries: 100
